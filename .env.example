# ReelSync (Vite env vars)
# Copy this file to .env and adjust values as needed.

# ICE/TURN config endpoint (POST) returning iceServers.
# Response format is similar to Cloudflare Realtime Kit.
VITE_TURN_SERVER_SOURCE_URL=

# Same-origin mode: max acceptable drift (seconds) before forced resync
VITE_MAX_ACCEPTABLE_DELAY_SECONDS=3

# P2P mode: RTT measurement interval (seconds)
VITE_LATENCY_MEASUREMENT_INTERVAL_SECONDS=2

# Same-origin mode: progress sync interval (seconds)
VITE_SAME_ORIGIN_SYNC_INTERVAL_SECONDS=1

# Optional: inject AdSense meta into index.html at build time
# VITE_ADSENSE_ACCOUNT=
